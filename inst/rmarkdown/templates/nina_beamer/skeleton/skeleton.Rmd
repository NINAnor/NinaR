---
title: "En lite lengre titell"
subtitle: "Med mer detaljer"
author: "For Efternavn"
date: "`r format(Sys.time(), '%d %b, %Y')`"
output:
  NinaR::ninaBeamer
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, fig.align = 'center', dev= 'pdf', out.width = '0.8\\linewidth', out.height = '2in')
```

## Norsk institutt for naturforskning

This template is experimental and under construction. Currently it resembles the default beamer template.

This is an R Markdown presentation. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document.

## Slide with Bullets

- Item 1
- Item 2
    - Item 2a
    - Item 2b

## Slide with R Output

```{r cars, echo = TRUE }
suppressPackageStartupMessages(require(NinaR))
barplot(runif(5), col=ninaPalette())
```

## Slide with Plot

```{r pressure, results='asis'}
suppressPackageStartupMessages(require(xtable))
print(xtable(head(cars)), comment=F)
```

## Two column test

\columnsbegin
\column{.5\textwidth}

```{r}
y<-5+5
y
```

\column{.5\textwidth}


```{r}
##This is good stuff
x <- y+10
```

\columnsend
